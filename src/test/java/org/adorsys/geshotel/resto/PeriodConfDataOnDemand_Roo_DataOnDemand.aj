// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.adorsys.geshotel.resto;

import java.lang.String;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.adorsys.geshotel.resto.domain.PeriodConf;
import org.springframework.stereotype.Component;

privileged aspect PeriodConfDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PeriodConfDataOnDemand: @Component;
    
    private Random PeriodConfDataOnDemand.rnd = new SecureRandom();
    
    private List<PeriodConf> PeriodConfDataOnDemand.data;
    
    public PeriodConf PeriodConfDataOnDemand.getNewTransientPeriodConf(int index) {
        PeriodConf obj = new PeriodConf();
        setClosingTime(obj, index);
        setDescription(obj, index);
        setOpeningTime(obj, index);
        setTitle(obj, index);
        setValidFrom(obj, index);
        setValidTo(obj, index);
        return obj;
    }
    
    public void PeriodConfDataOnDemand.setClosingTime(PeriodConf obj, int index) {
        int closingTime = index;
        obj.setClosingTime(closingTime);
    }
    
    public void PeriodConfDataOnDemand.setDescription(PeriodConf obj, int index) {
        String description = "description_" + index;
        obj.setDescription(description);
    }
    
    public void PeriodConfDataOnDemand.setOpeningTime(PeriodConf obj, int index) {
        int openingTime = index;
        obj.setOpeningTime(openingTime);
    }
    
    public void PeriodConfDataOnDemand.setTitle(PeriodConf obj, int index) {
        String title = "title_" + index;
        obj.setTitle(title);
    }
    
    public void PeriodConfDataOnDemand.setValidFrom(PeriodConf obj, int index) {
        Date validFrom = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setValidFrom(validFrom);
    }
    
    public void PeriodConfDataOnDemand.setValidTo(PeriodConf obj, int index) {
        Date validTo = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setValidTo(validTo);
    }
    
    public PeriodConf PeriodConfDataOnDemand.getSpecificPeriodConf(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        PeriodConf obj = data.get(index);
        return PeriodConf.findPeriodConf(obj.getId());
    }
    
    public PeriodConf PeriodConfDataOnDemand.getRandomPeriodConf() {
        init();
        PeriodConf obj = data.get(rnd.nextInt(data.size()));
        return PeriodConf.findPeriodConf(obj.getId());
    }
    
    public boolean PeriodConfDataOnDemand.modifyPeriodConf(PeriodConf obj) {
        return false;
    }
    
    public void PeriodConfDataOnDemand.init() {
        data = PeriodConf.findPeriodConfEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'PeriodConf' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<org.adorsys.geshotel.resto.domain.PeriodConf>();
        for (int i = 0; i < 10; i++) {
            PeriodConf obj = getNewTransientPeriodConf(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> it = e.getConstraintViolations().iterator(); it.hasNext();) {
                    ConstraintViolation<?> cv = it.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
