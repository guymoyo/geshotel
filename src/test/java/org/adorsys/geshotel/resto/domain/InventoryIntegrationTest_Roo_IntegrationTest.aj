// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.adorsys.geshotel.resto.domain;

import org.adorsys.geshotel.resto.domain.InventoryDataOnDemand;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect InventoryIntegrationTest_Roo_IntegrationTest {
    
    declare @type: InventoryIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: InventoryIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext.xml");
    
    declare @type: InventoryIntegrationTest: @Transactional;
    
    @Autowired
    private InventoryDataOnDemand InventoryIntegrationTest.dod;
    
    @Test
    public void InventoryIntegrationTest.testCountInventorys() {
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to initialize correctly", dod.getRandomInventory());
        long count = org.adorsys.geshotel.resto.domain.Inventory.countInventorys();
        org.junit.Assert.assertTrue("Counter for 'Inventory' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void InventoryIntegrationTest.testFindInventory() {
        org.adorsys.geshotel.resto.domain.Inventory obj = dod.getRandomInventory();
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to initialize correctly", obj);
        java.lang.Long id = obj.getId();
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to provide an identifier", id);
        obj = org.adorsys.geshotel.resto.domain.Inventory.findInventory(id);
        org.junit.Assert.assertNotNull("Find method for 'Inventory' illegally returned null for id '" + id + "'", obj);
        org.junit.Assert.assertEquals("Find method for 'Inventory' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void InventoryIntegrationTest.testFindAllInventorys() {
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to initialize correctly", dod.getRandomInventory());
        long count = org.adorsys.geshotel.resto.domain.Inventory.countInventorys();
        org.junit.Assert.assertTrue("Too expensive to perform a find all test for 'Inventory', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        java.util.List<org.adorsys.geshotel.resto.domain.Inventory> result = org.adorsys.geshotel.resto.domain.Inventory.findAllInventorys();
        org.junit.Assert.assertNotNull("Find all method for 'Inventory' illegally returned null", result);
        org.junit.Assert.assertTrue("Find all method for 'Inventory' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void InventoryIntegrationTest.testFindInventoryEntries() {
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to initialize correctly", dod.getRandomInventory());
        long count = org.adorsys.geshotel.resto.domain.Inventory.countInventorys();
        if (count > 20) count = 20;
        java.util.List<org.adorsys.geshotel.resto.domain.Inventory> result = org.adorsys.geshotel.resto.domain.Inventory.findInventoryEntries(0, (int) count);
        org.junit.Assert.assertNotNull("Find entries method for 'Inventory' illegally returned null", result);
        org.junit.Assert.assertEquals("Find entries method for 'Inventory' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void InventoryIntegrationTest.testFlush() {
        org.adorsys.geshotel.resto.domain.Inventory obj = dod.getRandomInventory();
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to initialize correctly", obj);
        java.lang.Long id = obj.getId();
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to provide an identifier", id);
        obj = org.adorsys.geshotel.resto.domain.Inventory.findInventory(id);
        org.junit.Assert.assertNotNull("Find method for 'Inventory' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyInventory(obj);
        java.lang.Integer currentVersion = obj.getVersion();
        obj.flush();
        org.junit.Assert.assertTrue("Version for 'Inventory' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void InventoryIntegrationTest.testMerge() {
        org.adorsys.geshotel.resto.domain.Inventory obj = dod.getRandomInventory();
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to initialize correctly", obj);
        java.lang.Long id = obj.getId();
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to provide an identifier", id);
        obj = org.adorsys.geshotel.resto.domain.Inventory.findInventory(id);
        boolean modified =  dod.modifyInventory(obj);
        java.lang.Integer currentVersion = obj.getVersion();
        org.adorsys.geshotel.resto.domain.Inventory merged =  obj.merge();
        obj.flush();
        org.junit.Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        org.junit.Assert.assertTrue("Version for 'Inventory' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void InventoryIntegrationTest.testPersist() {
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to initialize correctly", dod.getRandomInventory());
        org.adorsys.geshotel.resto.domain.Inventory obj = dod.getNewTransientInventory(Integer.MAX_VALUE);
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to provide a new transient entity", obj);
        org.junit.Assert.assertNull("Expected 'Inventory' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        org.junit.Assert.assertNotNull("Expected 'Inventory' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void InventoryIntegrationTest.testRemove() {
        org.adorsys.geshotel.resto.domain.Inventory obj = dod.getRandomInventory();
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to initialize correctly", obj);
        java.lang.Long id = obj.getId();
        org.junit.Assert.assertNotNull("Data on demand for 'Inventory' failed to provide an identifier", id);
        obj = org.adorsys.geshotel.resto.domain.Inventory.findInventory(id);
        obj.remove();
        obj.flush();
        org.junit.Assert.assertNull("Failed to remove 'Inventory' with identifier '" + id + "'", org.adorsys.geshotel.resto.domain.Inventory.findInventory(id));
    }
    
}
