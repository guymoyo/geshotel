// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.adorsys.geshotel.booking.domain;

import java.lang.String;
import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.adorsys.geshotel.booking.domain.OtherService;
import org.springframework.stereotype.Component;

privileged aspect OtherServiceDataOnDemand_Roo_DataOnDemand {
    
    declare @type: OtherServiceDataOnDemand: @Component;
    
    private Random OtherServiceDataOnDemand.rnd = new SecureRandom();
    
    private List<OtherService> OtherServiceDataOnDemand.data;
    
    public OtherService OtherServiceDataOnDemand.getNewTransientOtherService(int index) {
        OtherService obj = new OtherService();
        setAmount(obj, index);
        setName(obj, index);
        setServiceDesc(obj, index);
        return obj;
    }
    
    public void OtherServiceDataOnDemand.setAmount(OtherService obj, int index) {
        BigDecimal amount = BigDecimal.valueOf(index);
        obj.setAmount(amount);
    }
    
    public void OtherServiceDataOnDemand.setName(OtherService obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public void OtherServiceDataOnDemand.setServiceDesc(OtherService obj, int index) {
        String serviceDesc = "serviceDesc_" + index;
        obj.setServiceDesc(serviceDesc);
    }
    
    public OtherService OtherServiceDataOnDemand.getSpecificOtherService(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        OtherService obj = data.get(index);
        return OtherService.findOtherService(obj.getId());
    }
    
    public OtherService OtherServiceDataOnDemand.getRandomOtherService() {
        init();
        OtherService obj = data.get(rnd.nextInt(data.size()));
        return OtherService.findOtherService(obj.getId());
    }
    
    public boolean OtherServiceDataOnDemand.modifyOtherService(OtherService obj) {
        return false;
    }
    
    public void OtherServiceDataOnDemand.init() {
        data = OtherService.findOtherServiceEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'OtherService' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<org.adorsys.geshotel.booking.domain.OtherService>();
        for (int i = 0; i < 10; i++) {
            OtherService obj = getNewTransientOtherService(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> it = e.getConstraintViolations().iterator(); it.hasNext();) {
                    ConstraintViolation<?> cv = it.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
