// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.adorsys.geshotel.booking.domain;

import java.lang.String;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.adorsys.geshotel.booking.domain.Room;
import org.adorsys.geshotel.booking.domain.RoomCategory;
import org.adorsys.geshotel.booking.domain.RoomCategoryDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect RoomDataOnDemand_Roo_DataOnDemand {
    
    declare @type: RoomDataOnDemand: @Component;
    
    private Random RoomDataOnDemand.rnd = new SecureRandom();
    
    private List<Room> RoomDataOnDemand.data;
    
    @Autowired
    private RoomCategoryDataOnDemand RoomDataOnDemand.roomCategoryDataOnDemand;
    
    public Room RoomDataOnDemand.getNewTransientRoom(int index) {
        Room obj = new Room();
        setCapacity(obj, index);
        setCoordinates(obj, index);
        setDescription(obj, index);
        setPhoneNumber(obj, index);
        setRoomCategory(obj, index);
        setRoomNumber(obj, index);
        return obj;
    }
    
    public void RoomDataOnDemand.setCapacity(Room obj, int index) {
        String capacity = "capacity_" + index;
        obj.setCapacity(capacity);
    }
    
    public void RoomDataOnDemand.setCoordinates(Room obj, int index) {
        String coordinates = "coordinates_" + index;
        obj.setCoordinates(coordinates);
    }
    
    public void RoomDataOnDemand.setDescription(Room obj, int index) {
        String description = "description_" + index;
        if (description.length() > 255) {
            description = description.substring(0, 255);
        }
        obj.setDescription(description);
    }
    
    public void RoomDataOnDemand.setPhoneNumber(Room obj, int index) {
        String phoneNumber = "phoneNumber_" + index;
        obj.setPhoneNumber(phoneNumber);
    }
    
    public void RoomDataOnDemand.setRoomCategory(Room obj, int index) {
        RoomCategory roomCategory = roomCategoryDataOnDemand.getRandomRoomCategory();
        obj.setRoomCategory(roomCategory);
    }
    
    public void RoomDataOnDemand.setRoomNumber(Room obj, int index) {
        String roomNumber = "roomNumber_" + index;
        obj.setRoomNumber(roomNumber);
    }
    
    public Room RoomDataOnDemand.getSpecificRoom(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        Room obj = data.get(index);
        return Room.findRoom(obj.getId());
    }
    
    public Room RoomDataOnDemand.getRandomRoom() {
        init();
        Room obj = data.get(rnd.nextInt(data.size()));
        return Room.findRoom(obj.getId());
    }
    
    public boolean RoomDataOnDemand.modifyRoom(Room obj) {
        return false;
    }
    
    public void RoomDataOnDemand.init() {
        data = Room.findRoomEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'Room' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<org.adorsys.geshotel.booking.domain.Room>();
        for (int i = 0; i < 10; i++) {
            Room obj = getNewTransientRoom(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> it = e.getConstraintViolations().iterator(); it.hasNext();) {
                    ConstraintViolation<?> cv = it.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
