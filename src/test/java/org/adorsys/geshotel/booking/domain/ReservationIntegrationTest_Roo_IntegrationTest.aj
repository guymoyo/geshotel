// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.adorsys.geshotel.booking.domain;

import org.adorsys.geshotel.booking.domain.ReservationDataOnDemand;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ReservationIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ReservationIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ReservationIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext.xml");
    
    declare @type: ReservationIntegrationTest: @Transactional;
    
    @Autowired
    private ReservationDataOnDemand ReservationIntegrationTest.dod;
    
    @Test
    public void ReservationIntegrationTest.testCountReservations() {
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to initialize correctly", dod.getRandomReservation());
        long count = org.adorsys.geshotel.booking.domain.Reservation.countReservations();
        org.junit.Assert.assertTrue("Counter for 'Reservation' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ReservationIntegrationTest.testFindReservation() {
        org.adorsys.geshotel.booking.domain.Reservation obj = dod.getRandomReservation();
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to initialize correctly", obj);
        java.lang.Long id = obj.getId();
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to provide an identifier", id);
        obj = org.adorsys.geshotel.booking.domain.Reservation.findReservation(id);
        org.junit.Assert.assertNotNull("Find method for 'Reservation' illegally returned null for id '" + id + "'", obj);
        org.junit.Assert.assertEquals("Find method for 'Reservation' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ReservationIntegrationTest.testFindAllReservations() {
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to initialize correctly", dod.getRandomReservation());
        long count = org.adorsys.geshotel.booking.domain.Reservation.countReservations();
        org.junit.Assert.assertTrue("Too expensive to perform a find all test for 'Reservation', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        java.util.List<org.adorsys.geshotel.booking.domain.Reservation> result = org.adorsys.geshotel.booking.domain.Reservation.findAllReservations();
        org.junit.Assert.assertNotNull("Find all method for 'Reservation' illegally returned null", result);
        org.junit.Assert.assertTrue("Find all method for 'Reservation' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ReservationIntegrationTest.testFindReservationEntries() {
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to initialize correctly", dod.getRandomReservation());
        long count = org.adorsys.geshotel.booking.domain.Reservation.countReservations();
        if (count > 20) count = 20;
        java.util.List<org.adorsys.geshotel.booking.domain.Reservation> result = org.adorsys.geshotel.booking.domain.Reservation.findReservationEntries(0, (int) count);
        org.junit.Assert.assertNotNull("Find entries method for 'Reservation' illegally returned null", result);
        org.junit.Assert.assertEquals("Find entries method for 'Reservation' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ReservationIntegrationTest.testFlush() {
        org.adorsys.geshotel.booking.domain.Reservation obj = dod.getRandomReservation();
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to initialize correctly", obj);
        java.lang.Long id = obj.getId();
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to provide an identifier", id);
        obj = org.adorsys.geshotel.booking.domain.Reservation.findReservation(id);
        org.junit.Assert.assertNotNull("Find method for 'Reservation' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyReservation(obj);
        java.lang.Integer currentVersion = obj.getVersion();
        obj.flush();
        org.junit.Assert.assertTrue("Version for 'Reservation' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ReservationIntegrationTest.testMerge() {
        org.adorsys.geshotel.booking.domain.Reservation obj = dod.getRandomReservation();
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to initialize correctly", obj);
        java.lang.Long id = obj.getId();
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to provide an identifier", id);
        obj = org.adorsys.geshotel.booking.domain.Reservation.findReservation(id);
        boolean modified =  dod.modifyReservation(obj);
        java.lang.Integer currentVersion = obj.getVersion();
        org.adorsys.geshotel.booking.domain.Reservation merged =  obj.merge();
        obj.flush();
        org.junit.Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        org.junit.Assert.assertTrue("Version for 'Reservation' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ReservationIntegrationTest.testPersist() {
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to initialize correctly", dod.getRandomReservation());
        org.adorsys.geshotel.booking.domain.Reservation obj = dod.getNewTransientReservation(Integer.MAX_VALUE);
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to provide a new transient entity", obj);
        org.junit.Assert.assertNull("Expected 'Reservation' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        org.junit.Assert.assertNotNull("Expected 'Reservation' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ReservationIntegrationTest.testRemove() {
        org.adorsys.geshotel.booking.domain.Reservation obj = dod.getRandomReservation();
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to initialize correctly", obj);
        java.lang.Long id = obj.getId();
        org.junit.Assert.assertNotNull("Data on demand for 'Reservation' failed to provide an identifier", id);
        obj = org.adorsys.geshotel.booking.domain.Reservation.findReservation(id);
        obj.remove();
        obj.flush();
        org.junit.Assert.assertNull("Failed to remove 'Reservation' with identifier '" + id + "'", org.adorsys.geshotel.booking.domain.Reservation.findReservation(id));
    }
    
}
